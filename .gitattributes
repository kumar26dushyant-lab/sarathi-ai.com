# Shell scripts MUST keep LF endings — a CRLF shebang breaks execution on the Linux
# server (systemd reports 203/EXEC). This applies regardless of the dev box's core.autocrlf.
*.sh text eol=lf
deploy/*.sh text eol=lf
