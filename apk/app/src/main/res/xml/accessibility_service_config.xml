<?xml version="1.0" encoding="utf-8"?>
<!--
    Minimal accessibility service config.
    WAAccessibilityService is declared for future proactive-send capability
    but is NOT required for the core notification-reply flow.
    Users do not need to enable this for basic operation.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:description="@string/app_name"
    android:notificationTimeout="100"
    android:packageNames="com.whatsapp,com.whatsapp.w4b"
    android:settingsActivity=".ui.StatusActivity" />
